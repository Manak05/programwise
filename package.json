{
  "name": "programwise",
  "private": true,
  "version": "1.0.0",
  "description": "ProgramWise - full-stack education program decision-support platform",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
